services:
- type: web
  name: city-level-real-time-public-transit-schedules
  env: python
  plan: free
  buildCommand: pip install -r requirements.txt
  startCommand: gunicorn main:app
  autoDeploy: true
